{
  "name": "mega-todo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently --kill-others \"npm run dev -w packages/frontend\" \"npm run dev -w packages/api\"",
    "build": "npm run build -w packages/frontend && npm run build -w packages/api",
    "deploy": "npm run deploy -w packages/api && npm run deploy -w packages/frontend",
    "typecheck": "npm run typecheck -w packages/frontend && npm run typecheck -w packages/api"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
